数据类型
    数字
        byte(8),short(16),int(32),long(64),float(32),double(64)
    字符
        char
    真伪
        boolean

    类型转换
        规则：低->高，系统自动类型转换；高->低，需强制转换
            i.e. 使String->int: Integer.parseInt(String)
        阶级关系：byte==short==char<int<float<long==double
